package flexscript.features.discordRPC;


public class DiscordRPCManager {

    public static void discordRPC(){/* 
        DiscordRPC discord = DiscordRPC.INSTANCE;
        String applicationId = "939248929562824744";
        String steamId = "";

        DiscordEventHandlers handlers = new DiscordEventHandlers();
        discord.Discord_Initialize(applicationId, handlers, true, steamId);

        DiscordRichPresence presence = new DiscordRichPresence();
        presence.startTimestamp = System.currentTimeMillis() / 1000;
        presence.details = "FlexScript";
        presence.state = "Playing HYPIXEL";
        presence.largeImageKey = "unknown";
        presence.largeImageText = "FlexScript PREMIUM";
        discord.Discord_UpdatePresence(presence);
        
        new Thread(() -> {
            while (!Thread.currentThread().isInterrupted()) {
                try {
                    Thread.sleep(2000);
                } catch (InterruptedException ignored) {}
            }
        }, "RPC-Callback-Handler").start(); */
    }



    

    
}
    
